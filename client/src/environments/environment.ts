// Development environment — replaced by environment.production.ts for prod builds
export const environment = {
  production: false,
  apiUrl: ''   // Empty: Angular dev-server proxy forwards /api/* to backend
};
